import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CalorieTracker = () => {
  return (
    <View>
      <Text>CalorieTracker</Text>
    </View>
  );
};

export default CalorieTracker;

const styles = StyleSheet.create({});
